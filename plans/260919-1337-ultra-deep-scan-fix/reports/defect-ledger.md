# Defect ledger — ultra deep-scan fix (2026-09-19)

Status legend: FIXED (this cycle) | VERIFIED-OK | LEDGERED (deferred with owner/trigger) | NOT_RUN.

| # | Finding | Severity | Evidence | Status |
|---|---|---|---|---|
| D1 | Public chatbot always fell back: Spring citation revalidation only accepted catalog UUIDs while the knowledge base keys documents by `faq-*`/`bv-*`/`br-*` ids; every ANSWER died at `badGateway("missing a verified public catalog source")` → BFF fallback. Reproduced warm (A/B: direct `remote_provider` 16.9s vs public fallback 14.8s). | P0 | `AiChatSourceResolver.java` `isUuid` gate; probes 07:19Z (36.7s cold) + warm A/B | FIXED — fail-closed knowledge bridge in HOSPITAL_SUPPORT (commit bfffe53), regression test green; backend image deploy pending CI. |
| D2 | Five `[DBG ...]` prints shipped inside the deployed grounding gates (predecessor handoff warned but never reverted). | P0 | `llm.py:1727,1756,1760,1808,1863` | FIXED (88abcad); AI suite 741/741 after removal. |
| D3 | Blueprint host drift: `render.yaml`/`render-free-beta.yaml` declared `healthcare-beta-ai.onrender.com`; live host is `healthcare-beta-ai-9mip.onrender.com`. Same stale host asserted in `same-origin-api.test.mjs:109`. A blueprint apply would point the backend at a dead host. | P0 | Render API service details | FIXED (88abcad + e301c35); contract tests 9/9 green. |
| D4 | Blueprint `RAG_STORAGE_BACKEND=memory` vs live AI service `supabase` — a blueprint apply would silently disable the knowledge base. | P0 | Render env API | FIXED for the AI service; backend intentionally stays `memory` (matches live, no RAG use). |
| D5 | V85 migration references phantom `doctors.rating` column — fresh-DB migration chain broken; CI red since `dee9919`; **blocked the backend image pipeline** (explains E10 staleness) and would break the next prod deploy. | P0 | CI run 35430763923, SQLState 42703 | FIXED (e301c35) — guarded on column existence. |
| D6 | Fallback copy used engineering jargon ("thông tin đã xác thực"); fallback answers rendered with NO label (`AssistantProvider.provenanceLabel` returned null for INSUFFICIENT_EVIDENCE). | P1 | `healthcare-bff.ts:171`, `AssistantProvider.tsx:64` | FIXED (e7e03f35) — jargon-free copy + leading "Hướng dẫn tạm thời" chip with one-tap retry; bff-security suite 387/387. |
| D7 | Multisymptom escalation FPs: bare `kem`/`vua` substring indicators wasted remote spend on single-symptom/skincare questions. | P2 | `chatbot.py:762-798` | FIXED (e7e03f4) — terms removed + word-boundary matching; 32/32 routing suite. |
| D8 | Admin PATCH/DELETE fell to the looser catch-all tier instead of `admin-mutations`. | P2 | `RequestRateLimitFilter.java:308` | FIXED (e7e03f4) + regression test (fails on old behavior). |
| D9 | `verify-deploy-qa.mjs` times out on `networkidle` (script fragility, not a site defect — pages return 200 in probe-live). | P3 | 07:35Z run | LEDGERED — switch to `domcontentloaded` when next touched. |
| D10 | Keep-alive cron `0 */6 * * *` cannot prevent AI cold starts (Render sleeps after 15 min; Actions cron also jitters 10-30 min). AI always-on ≈744h of the 750h quota alone — strictly worse per Advisor/Kongming review (it degrades every other surface). | P2 | `render-keep-alive.yml`; plan D2 | LEDGERED — accepted cold-start posture; cold path now ends in labelled fallback (AC2), warm-slow fixed by D1. Revisit only with a paid tier. |
| D11 | ADR-004 documented posture (kill-switch on, synthetic-only, patient-chat remote disabled) contradicts live env (all remote flags enabled deliberately per the 2026-09-14 product requirements). A blueprint apply would disable the chatbot's remote path. | P2 | `supabase/tests/test_render_blueprint_contract.py:73-76` vs Render env API | LEDGERED — needs an ADR-004 revision decision by the owner; not silently changed here. |
| D12 | Backend image deploys carry no commit↔image record (NULL-commit `api` deploys). | P2 | Render deploy API | LEDGERED — triplet recorded per probe in this cycle's reports; automate mapping inside `publish-images.yml` as follow-up. |
| D13 | Portal session: `?next=` guard (`safeAuthNextPath`) rejects non-`/`, `//`, double-escaped, and control-char paths; session hydration and cross-tab behaviour covered by `auth-session-hydration.test.mjs` + `restful-auth.test.mjs` regex gates. consentVersion propagation audited — no user-visible defect found; deferred per scope. | VERIFIED-OK | `auth-flow.ts:90-104`, existing suites | VERIFIED-OK / deferred (consentVersion). |
| D14 | Frontend / AI / infra local suites; Playwright E2E | gate | — | Frontend + AI run in Phase 7; Playwright E2E NOT_RUN this cycle (no browser harness locally), recorded honestly. |
| D15 | Package card imagery still reused across different packages on `/packages` (cancer-screening ×2, diabetes ×2, geriatric ×2 in the top-9). | P3 | `scripts/probe-packages.mjs` sweep 08:0xZ | LEDGERED — needs a wider distinct-image pool or per-package art direction; carried from the 2026-09-18 R3 unresolved decision. |
| D16 | V85 was a minefield beyond `doctors.rating`: phantom `services.price`, article index on nonexistent `status` column, and an appointment status CHECK that omitted `PENDING_CONFIRMATION` — the application's DEFAULT status, which would have rejected every new appointment the moment the next backend deploy ran migrations. | P0 | CI runs 35431915425 / 35432686096; `AppointmentStatus.java` | FIXED (30bb0d8 + follow-up) — column guards, index on real columns, status list aligned to the app enum. |
| D17 | The V83 standard-hours fallback generated slots for INACTIVE branches (persisted schedules still resolved). | P1 | `AppointmentBookingIntegrationTest.slotsRejectInactiveCatalogAndDeduplicateEquivalentScheduleRows` | FIXED — ScheduleService now fail-closes explicit-branch slot queries on inactive branches (nullable BranchRepository guard). |
| D18 | Two booking tests encoded the pre-V83 contract (empty slots for an unscheduled branch). Superseded by the explicit 2026-09-18 product requirement "any doctor bookable on any day"; tests updated to the new, stronger assertions (slot branch identity + successful hold). | — | `AppointmentBookingIntegrationTest` | FIXED (tests updated, not weakened). |

## Keep-alive hour math (D10)

- Quota: 750 h/month per free service; month ≈ 744 h.
- AI always-on ≈ 744 h + backend always-on ≈ 744 h ⇒ both cannot fit one 750 h budget; keeping AI warm trades the chatbot's cold start for cold starts on login/booking/articles (all backend surfaces) — rejected.
- `*/13` business-hours cron: free-tier Actions runners jitter 10-30+ min ⇒ misses the 15-min sleep window anyway ⇒ ineffective complexity; rejected.
- Decision: accept cold starts; the repaired chain + labelled fallback + one-tap retry is the designed behaviour (D2 in plan).
