package com.healthcare.ai.chat.entity;

/** Immutable intent selected when a patient conversation is created. */
public enum ChatMode {
    HOSPITAL_SUPPORT,
    SYMPTOM_TRIAGE,
    HEALTH_EDUCATION
}
